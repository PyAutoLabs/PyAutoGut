## pointmass-smbh-jax-ci
- issue: https://github.com/PyAutoLabs/autolens_workspace_test/issues/251
- completed: 2026-08-06
- workspace-pr: https://github.com/PyAutoLabs/autolens_workspace_test/pull/252 (merged bb60ecc)
- summary: CI JAX coverage for the PointMass/SMBH regression class (follow-up to PyAutoGalaxy#553/#554). `misc/profiles_jit.py` gains `mp.PointMass` + `mp.SMBH` (deflections + potential via the three-step pattern on both grid types; raw-zeros convergence check since their convergence is undecorated by design). New `imaging/jax_likelihood/smbh.py` runs the full vmap+jit likelihood pipeline with the SMBH `centre`/`mass` FREE — a free traced mass is the only configuration that reaches the `SMBH.__init__` conversion inside the trace — and joins `smoke_tests.txt` (25.9s under the smoke env vs the 300s cap; `ENV: jax full_datasets` release proven via `build_env_for_script`).
- KEY FINDING (own prompt candidate): at this workspace's default prior medians (effective_radius=15.0, einstein_radius=4.0) the positive-only linear solver ZEROES the source's solved intensity, making the imaging likelihood BIT-IDENTICAL for any source-plane mass structure (verified: Isothermal θ_E 4.0 vs 2.0 and SMBH mass 1e9 vs 1e11 all → LL −352316.6109446431). Consequence 1: smbh.py pins its non-SMBH components to `simulator/simple.py` truth so its literal actually responds to the SMBH (LL 1187.6 → 1194.8 → −42.6 across masses). Consequence 2: EXISTING jax_likelihood literals generated at prior medians constrain source-plane deflections only weakly — a mass-pipeline regression would not move them. Worth a dedicated prompt.
- traps: chased a phantom "SMBH ignored by the fit" for five probes before the control (vary Isothermal θ_E at the same parameter point) revealed the zeroed-source mechanism — the insensitivity was the parameter point, not the composition path. `lp.py` control run FIRST reproduced its pinned literal, per the control-before-hand-rolling discipline.
- ship-context: GitHub Actions partial outage — no runs ever created for the PR (close/reopen included); merged on explicit human instruction backed by local validation. Heart RED (`release validation FAILED (stage integrate)`, unrelated release arc) under the same-day reason-set ack from the PyAutoGalaxy#554 ship. First CI execution of the new entry will be the post-recovery main smoke run — verify it.
- follow-up: PyAutoGalaxy main Tests run at 2026-08-06T15:23 was a FAILURE **pre-dating** the #554 merge (18:11) — triage once the Actions queue drains; the #554 branch runs were still queued at merge time.

## Original prompt

# CI JAX coverage for PointMass/SMBH in @autolens_workspace_test

Type: test
Target: autolens_workspace_test
Difficulty: small
Autonomy: safe
Priority: normal
Status: formalised

## Original request (verbatim)

> ok yes do a CI exercise in autolens_workspace_test with JAX on these profiles
> as a quick follow up

Follow-up to PyAutoGalaxy#553 / PR#554 (merged daf3b8d8): `al.mp.PointMass` and
`al.mp.SMBH` broke every JAX fit and nothing in CI exercised the JAX path for
point-mass profiles, so the regression class was invisible.

## Plan

Two touches in @autolens_workspace_test, covering both bug shapes:

1. `scripts/misc/profiles_jit.py` — add `mp.PointMass` and `mp.SMBH` to the
   mass-profile list (`deflections_yx_2d_from`, `convergence_2d_from` on both
   `Grid2DIrregular` and `Grid2D.uniform`, three-step JAX pattern). Catches the
   ArrayIrregular/deflections bug shape at the profile level. Runs in the full
   sweep / release profile (not in smoke_tests.txt).
2. New `scripts/imaging/jax_likelihood/smbh.py` — full-pipeline vmap+jit
   likelihood with lens = Isothermal + `SMBH` where `mass` is a FREE parameter
   (LogUniformPrior; redshifts fixed floats). A free traced mass is the ONLY
   way to catch the `SMBH.__init__` traced-`np.sqrt` bug shape —
   `profiles_jit.py` instantiates with floats and can never see it. Mirror
   `imaging/jax_likelihood/lp.py` exactly (auto-sim `jax_test` dataset,
   `__Env__` with `ENV: jax full_datasets`, hardcoded vmap literal generated by
   running the script, Path A jit round-trip vs numpy). Add to
   `smoke_tests.txt` so the PR CI gate runs it (300s cap; lp.py-scale cost).
3. Update `scripts/CLAUDE.md` (jax_likelihood table row + profiles_jit list).

Traps: run `lp.py` first as a CONTROL to confirm the local env reproduces its
pinned literal before generating ours; wipe `output/` between runs; the vmap
literal must be generated under the same env tokens CI applies.
