# `generate.py` deletes notebooks/ before rejecting an unknown project

Type: bug
Target: pyautohands
Repos:
- PyAutoHands
- PyAutoNerves
- autocti_workspace
Difficulty: small
Autonomy: supervised
Priority: normal
Status: formalised

Two defects, one destructive.

**1. Destructive ordering (the real bug).** `autohands/generate.py` clears the
`notebooks/` tree *before* validating the project name. Running it on
`autocti_workspace` deleted **117 tracked files** under `notebooks/` and left a
stray `.ipynb` inside `scripts/`, then aborted with:

```
ValueError: inject_colab_setup: unknown project 'autocti' — add it to
COLAB_PROJECTS here and to the _PROJECTS registry in PyAutoNerves's
autonerves/setup_colab.py.
Known: ['autofit', 'autogalaxy', 'autolens', 'howtofit', 'howtogalaxy', 'howtolens']
```

The failure message is good; the ordering is not. A tool that wipes a tracked
tree and *then* discovers it cannot proceed leaves the repo in a state only
`git checkout` recovers — and in a repo with uncommitted work, not even that.
Validate the project (and anything else that can fail fast) **before** deleting
anything.

Encountered while shipping autolens_workspace#377 (script-docstring reference
sweep). Recovered with `git checkout -- notebooks` plus removing the stray file;
no damage shipped, and `autocti_workspace#15` therefore carries its 5 script
fixes with notebooks left untouched.

**2. `autocti` is not a registered project.** `COLAB_PROJECTS` in
`PyAutoHands/autohands/build_util.py:37` and `_PROJECTS` in
`PyAutoNerves/autonerves/setup_colab.py` both omit it, so
`autocti_workspace`'s notebooks cannot be regenerated by the standard tool at
all. Either register it (if its notebooks are meant to carry Colab setup) or
give `generate.py` a documented no-Colab mode for workspaces that do not — and
say which in `autocti_workspace/AGENTS.md`, whose *Notebook regeneration* line
currently implies the standard path works.

Fixing (1) is the priority: it is a data-loss footgun independent of `autocti`.
Any future unregistered project hits the same trap.

## Original request

Filed from the `script_prose_and_howto_ref_drift` sweep, where the crash was hit
directly. Not part of that task's scope.
